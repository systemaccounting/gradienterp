{
  "type": "object",
  "description": "Decline a buyer's proposed purchase order (the seller saying no). Terminal: the row is marked declined on both sides and never settles. To offer different terms instead, use return_quote.",
  "properties": {
    "thread": {"type": "string", "description": "The PO thread being declined (from the inbound po.proposed)."},
    "terms_hash": {"type": "string", "description": "The exact terms being declined, echoed from the proposal."}
  },
  "required": ["thread", "terms_hash"]
}
