# invoicing

## why this replaces the billing UI

same argument as contacts: the SaaS sells the UI over a crud schema + workflow state. the agent reads rows, writes rows, posts the books, runs the follow-ups. the per-seat fee is the UI tax.
