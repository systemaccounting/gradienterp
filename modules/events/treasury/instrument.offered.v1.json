{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/treasury/instrument.offered.v1",
  "title": "instrument.offered",
  "description": "SPECULATIVE. An instrument listed for sale. Matches buy-side capital — firms/holders with idle cash. Sold like a commodity on the PO rail: no broker to skim or fat-finger.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "instrument_id",
    "kind",
    "price",
    "terms"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "instrument_id": {
      "type": "string",
      "minLength": 1,
      "description": "The instrument."
    },
    "kind": {
      "type": "string",
      "minLength": 1,
      "description": "Instrument kind."
    },
    "price": {
      "type": "number",
      "description": "Asking price.",
      "exclusiveMinimum": 0
    },
    "terms": {
      "type": "string",
      "minLength": 1,
      "description": "Free-form terms summary."
    }
  }
}
