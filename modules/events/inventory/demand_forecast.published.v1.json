{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/inventory/demand_forecast.published.v1",
  "title": "demand_forecast.published",
  "description": "SPECULATIVE. A firm publishes expected consumption — the supplier's order book (collaborative planning).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "catalog_key",
    "horizon_days",
    "quantities",
    "location"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "catalog_key": {
      "type": "string",
      "minLength": 1,
      "description": "Location-free SKU."
    },
    "horizon_days": {
      "type": "integer",
      "minimum": 1,
      "description": "Forecast horizon."
    },
    "quantities": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "number",
        "description": "Expected qty per period."
      },
      "description": "Per-period expected quantities across the horizon."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
