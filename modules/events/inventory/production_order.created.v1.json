{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/inventory/production_order.created.v1",
  "title": "production_order.created",
  "description": "SPECULATIVE. A build is planned (PRODUCED forthcoming); an op marked for outsourcing matches firms with idle work-center capacity.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "order_id",
    "item_id",
    "qty"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "order_id": {
      "type": "string",
      "minLength": 1,
      "description": "Platform-generated id."
    },
    "item_id": {
      "type": "string",
      "minLength": 1,
      "description": "The composite being built."
    },
    "qty": {
      "type": "number",
      "description": "Units to build.",
      "exclusiveMinimum": 0
    },
    "needed_by_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "0 = whenever."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
