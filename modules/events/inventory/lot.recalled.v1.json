{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/inventory/lot.recalled.v1",
  "title": "lot.recalled",
  "description": "SPECULATIVE. A lot is recalled; urgency flows DOWNSTREAM (inverted match): consumers are firms whose movements contain the lot. Cross-firm traceability conventional ERP can't do — the chain crosses firm boundaries.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "lot_id",
    "catalog_keys",
    "reason",
    "severity"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "lot_id": {
      "type": "string",
      "minLength": 1,
      "description": "The recalled lot."
    },
    "catalog_keys": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "Affected SKU."
      },
      "description": "SKUs the lot shipped under."
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "description": "Why."
    },
    "severity": {
      "type": "string",
      "enum": [
        "advisory",
        "mandatory"
      ],
      "description": "advisory = check; mandatory = quarantine now."
    }
  }
}
