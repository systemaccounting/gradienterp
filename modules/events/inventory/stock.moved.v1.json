{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/inventory/stock.moved.v1",
  "title": "stock.moved",
  "description": "SPECULATIVE (emit call pending). Mirror of a @point movement: the physical i/o series on the visibility plane. Aggregates feed demand curves and the public cost feed.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "item_id",
    "delta",
    "movement_type",
    "at_ms",
    "location"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "item_id": {
      "type": "string",
      "minLength": 1,
      "description": "Full location-prefixed item id (<n>#<sku>)."
    },
    "delta": {
      "type": "number",
      "description": "Signed quantity moved."
    },
    "movement_type": {
      "type": "string",
      "minLength": 1,
      "description": "SOLD | RECEIVED | ADJUSTED | PRODUCED"
    },
    "at_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Effective time."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
