{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/inventory/capacity.idle.v1",
  "title": "capacity.idle",
  "description": "SPECULATIVE. A capacity item's forward window is unbooked (the parking-lot signal). Matches production orders / jobs / reservations needing that capacity; feeds the spare-capacity index.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "item_id",
    "capacity_class",
    "windows",
    "location"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "item_id": {
      "type": "string",
      "minLength": 1,
      "description": "The capacity item."
    },
    "capacity_class": {
      "type": "string",
      "minLength": 1,
      "description": "What kind: 'bay', 'room', 'chair', 'vehicle', 'work_center'."
    },
    "windows": {
      "type": "array",
      "minItems": 1,
      "description": "Free intervals.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "from_ms",
          "to_ms"
        ],
        "properties": {
          "from_ms": {
            "type": "integer",
            "minimum": 0,
            "description": "Window start."
          },
          "to_ms": {
            "type": "integer",
            "minimum": 0,
            "description": "Window end."
          }
        }
      }
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
