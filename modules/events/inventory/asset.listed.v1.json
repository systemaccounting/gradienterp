{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/inventory/asset.listed.v1",
  "title": "asset.listed",
  "description": "SPECULATIVE. A fixed asset retired outright (distinct from lease and from RMA/used goods). Matches purchase requests / capacity needs for the class.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "asset_id",
    "asset_class"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "asset_id": {
      "type": "string",
      "minLength": 1,
      "description": "The asset."
    },
    "asset_class": {
      "type": "string",
      "minLength": 1,
      "description": "e.g. 'espresso_machine', 'truck'."
    },
    "price": {
      "type": "number",
      "description": "Asking price; 0 = make an offer.",
      "minimum": 0
    },
    "condition": {
      "type": "string",
      "description": "Free-form condition note."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
