{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/invoicing/invoice.issued.v1",
  "title": "invoice.issued",
  "description": "SPECULATIVE (emit call pending). A receivable now exists. Matches financing offers — factoring as instrument-as-item on the existing PO rail.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "invoice_id",
    "total"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "invoice_id": {
      "type": "string",
      "minLength": 1,
      "description": "Invoice id (<n>#<id>, or a thread id cross-firm)."
    },
    "total": {
      "type": "number",
      "description": "Invoice total.",
      "exclusiveMinimum": 0
    },
    "due_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Due date; 0 = on receipt."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
