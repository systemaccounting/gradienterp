{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/invoicing/quotation.submitted.v1",
  "title": "quotation.submitted",
  "description": "SPECULATIVE. This firm's bid into a counterparty's rfq.issued window — the explicit chance to reprice.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "rfq_id",
    "price"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "rfq_id": {
      "type": "string",
      "minLength": 1,
      "description": "The RFQ bid on."
    },
    "price": {
      "type": "number",
      "description": "Offered total.",
      "exclusiveMinimum": 0
    },
    "valid_until_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Offer expiry; 0 = window close."
    },
    "terms": {
      "type": "string",
      "description": "Free-form terms."
    }
  }
}
