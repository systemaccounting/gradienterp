{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/labor/shift.clocked_in.v1",
  "title": "shift.clocked_in",
  "description": "SPECULATIVE (emit call pending). A shift opened. Feeds utilization aggregates; with shift.clocked_out, the labor half of the physical i/o series.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "worker_id",
    "role",
    "entry_id",
    "at_ms",
    "location"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "worker_id": {
      "class": "subject",
      "type": "string",
      "minLength": 1,
      "description": "Contact id."
    },
    "role": {
      "type": "string",
      "minLength": 1,
      "description": "Role worked."
    },
    "entry_id": {
      "type": "string",
      "minLength": 1,
      "description": "Time-entry id (<ts>#<n>#<id>)."
    },
    "at_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Clock-in time."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
