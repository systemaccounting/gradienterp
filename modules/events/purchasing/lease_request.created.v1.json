{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/purchasing/lease_request.created.v1",
  "title": "lease_request.created",
  "description": "SPECULATIVE. A firm seeks temporary use of an asset class (equipment, space). Matches underutilized assets across firms.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "asset_class",
    "from_ms",
    "to_ms"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "asset_class": {
      "type": "string",
      "minLength": 1,
      "description": "What kind of asset, e.g. 'forklift', 'parking'."
    },
    "from_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Lease start."
    },
    "to_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Lease end."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
