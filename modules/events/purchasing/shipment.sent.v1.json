{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/purchasing/shipment.sent.v1",
  "title": "shipment.sent",
  "description": "SPECULATIVE. Goods dispatched against a PO. Downstream: receipt booking; cross-firm freight matching reads the lane.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "po_id",
    "items"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "po_id": {
      "type": "string",
      "minLength": 1,
      "description": "The PO (or thread) shipped against."
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "description": "The requested/offered lines. catalog_key is the SKU half of an item id (location-free — the counterparty's own items live at their own locations).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "catalog_key",
          "qty"
        ],
        "properties": {
          "catalog_key": {
            "type": "string",
            "minLength": 1,
            "description": "SKU-ish key, e.g. 'green_colombia'"
          },
          "qty": {
            "type": "number",
            "description": "Quantity in the line's unit.",
            "exclusiveMinimum": 0
          },
          "unit": {
            "type": "string",
            "description": "Unit of measure; defaults to the item's own."
          }
        }
      }
    },
    "eta_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "Estimated arrival, ms-epoch; 0 = unknown."
    }
  }
}
