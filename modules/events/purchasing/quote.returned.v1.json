{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/purchasing/quote.returned.v1",
  "title": "quote.returned",
  "description": "A counterparty's answer to quote.requested, carried back on the bus.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "request_id",
    "price"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "request_id": {
      "type": "string",
      "minLength": 1,
      "description": "The quote.requested this answers."
    },
    "price": {
      "type": "number",
      "description": "Offered price.",
      "exclusiveMinimum": 0
    },
    "terms": {
      "type": "string",
      "description": "Free-form terms ('today 4pm', 'net 30')."
    }
  }
}
