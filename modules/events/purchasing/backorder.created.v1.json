{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/purchasing/backorder.created.v1",
  "title": "backorder.created",
  "description": "SPECULATIVE. A sale/build couldn't be filled from stock. Matches other firms' surplus of the same input and alternate suppliers' inventory.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "catalog_key",
    "qty"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "catalog_key": {
      "type": "string",
      "minLength": 1,
      "description": "The short SKU."
    },
    "qty": {
      "type": "number",
      "description": "Unfilled quantity.",
      "exclusiveMinimum": 0
    },
    "needed_by_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "0 = asap."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
