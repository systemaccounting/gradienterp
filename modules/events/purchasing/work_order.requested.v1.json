{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "modules/events/purchasing/work_order.requested.v1",
  "title": "work_order.requested",
  "description": "SPECULATIVE. A service need ('need a tech today') — matches firms with the trade + open capacity/shifts. The laundromat case as an event instead of a hub chat.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "openly_operated",
    "customer_id",
    "work_id",
    "trade",
    "description"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1,
      "description": "Bumped on breaking shape change. Consumers branch on it."
    },
    "openly_operated": {
      "type": "boolean",
      "description": "Set at emit time from the gerp's settings. The publication rule's filter — a private gerp's events never leave the operator plane."
    },
    "customer_id": {
      "type": "string",
      "minLength": 1,
      "description": "Emitting tenant (gerp_id). Redundant with the envelope account id but saves consumers a join."
    },
    "work_id": {
      "type": "string",
      "minLength": 1,
      "description": "Platform-generated id."
    },
    "trade": {
      "type": "string",
      "minLength": 1,
      "description": "Trade/capability sought, e.g. 'appliance-repair'."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "What needs doing."
    },
    "needed_by_ms": {
      "type": "integer",
      "minimum": 0,
      "description": "0 = asap."
    },
    "location": {
      "type": "string",
      "pattern": "^[0-9]+$",
      "description": "Location ordinal (the 1 in LOCATION#1#...). Present on placeful objects so cross-firm matches can be geo/loc-scoped; '1' = the emitter's main location."
    }
  }
}
