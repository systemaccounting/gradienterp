# contacts — why this replaces a CRM

Salesforce sells the UI. The UI renders rows from a CRUD schema and wraps writes in workflow state. The agent replaces the UI — reads rows, writes rows, chains context across conversations, runs follow-ups. The `$300/seat/month` is the UI tax.
