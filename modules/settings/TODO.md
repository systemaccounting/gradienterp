# settings — open work

- [ ] **non-owner `USER#<account_id>` rows** — created when a non-owner user is onboarded (labor module onboarding flow not built yet). Today only the owner's `USER` row is seeded at provisioning.
