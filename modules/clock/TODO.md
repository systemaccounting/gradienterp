# clock — open work

- **the settings read reaches only some functions.** `zone_name` resolves `GERP#timezone` when the function has `SETTINGS_TABLE` and `GetItem` on it; a function without them cuts periods and stamps metric records on `GERP_TIMEZONE`, the provision-time value. invoicing, rules, purchasing and shipping set no `SETTINGS_TABLE`, so an owner's later change on the gerp screen never reaches their period cuts. found 2026-09-22 when gradienterp's door (provisioned `UTC`, row `America/Los_Angeles`) stamped UTC days on the platform's count; the door has the read now, the four modules do not
