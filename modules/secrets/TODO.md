# secrets — open work

What's live is in [`AGENTS.md`](AGENTS.md) (`## current features`). Open work below.

- [ ] **one-shot cleanup** — a consuming tool deletes a setup secret after use (e.g. `configure_webhook` dropping the setup key once it has the `whsec_`) by invoking `manage_secret`'s delete; today the owner asks the agent.
