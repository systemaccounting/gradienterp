jsonschema

# Local AWS for the tests that run the REAL boto3 path (modules/aws/aws.py points them at
# an endpoint when AWS_LAMBDA_FUNCTION_NAME is absent). scripts/test.sh starts it if it is not
# already up. moto over LocalStack: Apache-2.0 with no licence gate, pure Python, no Docker.
moto[server]>=5.0

# tests/server builds the three local stacks as FastAPI apps (tests/server/_image.py), and the
# suites that exercise a route import it
fastapi

# modules/metrics reads run on duckdb locally, over the same Parquet the cabinet holds; the
# deployed function runs Athena and never imports it
duckdb
