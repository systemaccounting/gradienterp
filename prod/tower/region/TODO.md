# region — open work

- [ ] the reads on an Asian gerp: a vend into ap-southeast-1 or ap-south-1 (Sonnet 4 on the
      `apac.` profile), then chat, an addressed event from gradienterp, the investigator's read
      of its logs, its first hosting invoice
- [ ] the Irish gerp's first hosting invoice at month end (`bill_customer` reads the invoice
      unit `gerp-dublin-test-roasters-d542eb`)
- [ ] Textract is not in Tokyo or São Paulo: `inspect_document` there reads through a
      neighbouring region or is off; decide when such a gerp exists
- [ ] the Sonnet 5 flip, when `tmp/bedrock-probe-sonnet5.sh` reads open: one `model` entry per
      region — `us.` and `eu.` have Sonnet 5 profiles; Singapore and Mumbai have only `global.`
      for it (inference leaves the geography), Tokyo and Sydney neither yet
- a gerp does not move regions: a region is an account's, and a move is an export and a new gerp
